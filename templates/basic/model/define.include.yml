includes:
  #TODO 考虑把这种形式制定为默认甚至唯一形式。
  # 改成唯一模式： includes: - '@quick-qui/model-defines/model', 
  # 或者不要显试的‘model‘，model作为一个魔数，或者作为一个配置写到package.json里面。
  - protocol: library
    resource:
      type: npm # package.json里面要定义
      module: "@quick-qui/model-defines"
      dir: "model"
